package com.example.weatherapp.presentation.contract.toolbar

/**
 * if a class (e.g. Fragment) implements this interface,
 * it means that it only serves to store other screens and is not displayed on the screen
 */
interface ScreenContainer